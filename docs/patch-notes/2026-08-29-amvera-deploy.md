# Инструкция деплоя на Amvera

Дата: 2026-08-29
Ветка: agent/debug-combat-markers-ui

## Суть

Добавлена поддержка российского хостинга Amvera: конфиг `amvera.yaml` и инструкция `docs/deploy-amvera.md`. Сборка идёт по уже существующему `Dockerfile` — один контейнер отдаёт сайт и API.

## Правила

Правила игры не менялись.

## Код

Файлы `amvera.yaml`, `docs/deploy-amvera.md`.

## API / контракт

Контракт для программ не менялся.

## UI

Интерфейс для игрока не менялся.

## Тесты

Локально: `pnpm run build:deploy`, затем Docker с `PORT=80`. На Amvera — после push проверить `/`, `/api/health` и создание комнаты.

## Миграции

Миграция сохранений не нужна.

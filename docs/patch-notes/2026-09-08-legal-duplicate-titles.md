# На страницах условий и конфиденциальности больше нет второго заголовка подряд

Дата: 2026-09-08
Ветка: agent/fix-legal-duplicate-titles

## Суть
На странице условий использования заголовок показывался два раза подряд: один раз в шапке страницы и ещё раз внутри текста документа. То же повторение было на странице политики конфиденциальности. Лишний заголовок из текста документа убран — на экране остаётся один.

## Правила
Правила игры не менялись.

## Код
- Клиент `@galaxy/client`: фрагменты `packages/client/public/legal/terms-of-use.html` и `privacy-policy.html` — удалён встроенный `h1`, заголовок остаётся в `pages/legal/terms.vue` и `pages/legal/privacy.vue`.

## API / контракт
Контракт для программ не менялся.

## UI
На `/legal/terms` и `/legal/privacy` видимый заголовок один: «Условия использования» и «Конфиденциальность» соответственно, без повтора сразу под ним.

## Тесты
Открыть `/legal/terms` и `/legal/privacy` — заголовок один раз.

## Миграции
Миграция сохранений не нужна.
